<Window x:Class="HeartRateMonitor.WorkoutSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Podsumowanie Treningu" 
        Height="480" Width="460"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">

    <Border Background="#111116" BorderBrush="#252530" BorderThickness="1.5" CornerRadius="12">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Stats Grid -->
                <RowDefinition Height="Auto" /> <!-- Action Button -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <Grid>
                    <ui:SymbolIcon Symbol="Star24" FontSize="28" Foreground="#D89B00" HorizontalAlignment="Left" Filled="True"/>
                    <TextBlock Text="Podsumowanie Treningu" FontSize="18" FontWeight="Bold" Foreground="#f0f0f5" Margin="36,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock x:Name="TxtWorkoutName" Text="Sesja z dnia ..." FontSize="11" Foreground="#a0a0b0" Margin="0,4,0,0"/>
                <Border BorderThickness="0,1,0,0" BorderBrush="#252530" Margin="0,10,0,0" />
            </StackPanel>

            <!-- Stats Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                <StackPanel Margin="0,0,12,0">
                    <!-- Core Stats Grid -->
                    <UniformGrid Columns="2" Margin="0,0,0,16">
                        <Border Background="#191922" BorderThickness="1" BorderBrush="#252530" CornerRadius="8" Padding="12" Margin="0,0,6,6">
                            <StackPanel>
                                <TextBlock Text="CZAS TRENINGU" FontSize="9" FontWeight="Bold" Foreground="#a0a0b0" />
                                <TextBlock x:Name="TxtDuration" Text="00:00:00" FontSize="18" FontWeight="Bold" Foreground="#107C41" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <Border Background="#191922" BorderThickness="1" BorderBrush="#252530" CornerRadius="8" Padding="12" Margin="6,0,0,6">
                            <StackPanel>
                                <TextBlock Text="SPALONE KALORIE" FontSize="9" FontWeight="Bold" Foreground="#a0a0b0" />
                                <TextBlock x:Name="TxtCalories" Text="0 kcal" FontSize="18" FontWeight="Bold" Foreground="#D89B00" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <Border Background="#191922" BorderThickness="1" BorderBrush="#252530" CornerRadius="8" Padding="12" Margin="0,6,6,0">
                            <StackPanel>
                                <TextBlock Text="ŚREDNIE TĘTNO" FontSize="9" FontWeight="Bold" Foreground="#a0a0b0" />
                                <TextBlock x:Name="TxtAvgBpm" Text="-- BPM" FontSize="18" FontWeight="Bold" Foreground="#0078D4" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <Border Background="#191922" BorderThickness="1" BorderBrush="#252530" CornerRadius="8" Padding="12" Margin="6,6,0,0">
                            <StackPanel>
                                <TextBlock Text="MAKS. TĘTNO" FontSize="9" FontWeight="Bold" Foreground="#a0a0b0" />
                                <TextBlock x:Name="TxtMaxBpm" Text="-- BPM" FontSize="18" FontWeight="Bold" Foreground="#FF3B30" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <!-- Time in Zones Section -->
                    <TextBlock Text="CZAS W STREFACH TĘTNA" FontSize="11" FontWeight="Bold" Foreground="#a0a0b0" Margin="2,0,0,8"/>
                    
                    <StackPanel>
                        <!-- Zone 5 -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock Text="Strefa 5 (Ekstremalna)" FontSize="10" Foreground="#f0f0f5" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtZone5Time" Text="0s" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" x:Name="ProgressZone5" Minimum="0" Maximum="100" Value="0" Height="6" Background="#202028" Foreground="#9B59B6" Margin="0,2,0,6"/>
                        </Grid>

                        <!-- Zone 4 -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock Text="Strefa 4 (Beztlenowa)" FontSize="10" Foreground="#f0f0f5" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtZone4Time" Text="0s" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" x:Name="ProgressZone4" Minimum="0" Maximum="100" Value="0" Height="6" Background="#202028" Foreground="#E74C3C" Margin="0,2,0,6"/>
                        </Grid>

                        <!-- Zone 3 -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock Text="Strefa 3 (Cardio)" FontSize="10" Foreground="#f0f0f5" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtZone3Time" Text="0s" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" x:Name="ProgressZone3" Minimum="0" Maximum="100" Value="0" Height="6" Background="#202028" Foreground="#E67E22" Margin="0,2,0,6"/>
                        </Grid>

                        <!-- Zone 2 -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock Text="Strefa 2 (Spalanie tłuszczu)" FontSize="10" Foreground="#f0f0f5" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtZone2Time" Text="0s" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" x:Name="ProgressZone2" Minimum="0" Maximum="100" Value="0" Height="6" Background="#202028" Foreground="#F1C40F" Margin="0,2,0,6"/>
                        </Grid>

                        <!-- Zone 1 -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock Text="Strefa 1 (Rozgrzewka)" FontSize="10" Foreground="#f0f0f5" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtZone1Time" Text="0s" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" x:Name="ProgressZone1" Minimum="0" Maximum="100" Value="0" Height="6" Background="#202028" Foreground="#3498DB" Margin="0,2,0,6"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- Close Action Button -->
            <ui:Button Grid.Row="2" Content="Zamknij podsumowanie" Appearance="Primary" HorizontalAlignment="Stretch" Padding="0,10" Click="BtnClose_Click" />
        </Grid>
    </Border>
</Window>
