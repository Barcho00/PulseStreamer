<ui:FluentWindow x:Class="HeartRateMonitor.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Historia i Statystyki" Height="650" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#07070A">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0" Title="Historia i Statystyki" CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="History24" Foreground="#FF3B30" Filled="True" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16,0,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" /> <!-- List of historical sessions -->
                <ColumnDefinition Width="*" />   <!-- Selected session details & chart -->
            </Grid.ColumnDefinitions>

            <!-- Column 0: Historical sessions list -->
            <ui:Card Grid.Column="0" Margin="0,0,12,0" Padding="8" Background="#111116" BorderBrush="#1D1D26">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Margin="4,0,0,8">
                        <TextBlock Text="Historia Treningów" FontSize="13" FontWeight="SemiBold" Foreground="#a0a0b0" />
                        <TextBlock x:Name="TxtSelectedProfile" Text="Profil: --" FontSize="11" Foreground="#0078D4" Margin="0,2,0,0" />
                    </StackPanel>
                    
                    <ListView Grid.Row="1" x:Name="ListHistoricalSessions" SelectionChanged="ListHistoricalSessions_SelectionChanged" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#191922" BorderThickness="1" BorderBrush="#252530" CornerRadius="8" Padding="10,8" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="SemiBold" FontSize="12" Foreground="#f0f0f5" TextTrimming="CharacterEllipsis"/>
                                        <Grid Grid.Row="1" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding StartTimeText}" FontSize="10" Foreground="#a0a0b0" HorizontalAlignment="Left" />
                                            <TextBlock Text="{Binding AvgBpmText}" FontSize="10" FontWeight="SemiBold" Foreground="#0078D4" HorizontalAlignment="Right" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </ui:Card>

            <!-- Column 1: Details & Chart -->
            <Grid Grid.Column="1" x:Name="GridHistoryDetails" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /> <!-- Session Info & Export -->
                    <RowDefinition Height="Auto" /> <!-- KPI Cards Row -->
                    <RowDefinition Height="*" />    <!-- Chart -->
                </Grid.RowDefinitions>

                <!-- Session Info & Export -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding SelectedSession.Name}" FontSize="18" FontWeight="SemiBold" Foreground="#f0f0f5" />
                        <TextBlock Text="{Binding SelectedSession.StartTimeText}" FontSize="12" Foreground="#a0a0b0" Margin="0,4,0,0" />
                    </StackPanel>

                    <ui:Button Grid.Column="1" Content="Usuń" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Margin="0,0,8,0" Click="BtnDeleteSession_Click" Padding="12,6" />
                    <ui:Button Grid.Column="2" Content="Eksportuj CSV" Icon="{ui:SymbolIcon DocumentSave24}" Appearance="Secondary" Click="BtnExportCsvHistory_Click" Padding="12,6" />
                </Grid>

                <!-- KPI Cards Row -->
                <WrapPanel Grid.Row="1" Margin="0,0,0,12" Orientation="Horizontal">
                    <!-- Card 1: Workout Date -->
                    <ui:Card Margin="0,0,8,8" MinWidth="150" Padding="12" Background="#151311" BorderBrush="#D83B01" BorderThickness="1.5,1,1,1">
                        <Grid>
                            <ui:SymbolIcon Symbol="Calendar24" FontSize="50" Foreground="#D83B01" Opacity="0.1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-5,0"/>
                            <StackPanel>
                                <TextBlock Text="DATA TRENINGU" FontSize="9" FontWeight="Bold" Foreground="#e0af90" />
                                <TextBlock Text="{Binding SelectedSession.StartTimeText}" FontSize="12" FontWeight="Bold" Margin="0,12,0,0" Foreground="#D83B01" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>

                    <!-- Card 2: Average BPM -->
                    <ui:Card Margin="0,0,8,8" MinWidth="150" Padding="12" Background="#111316" BorderBrush="#0078D4" BorderThickness="1.5,1,1,1">
                        <Grid>
                            <ui:SymbolIcon Symbol="Heart24" Filled="False" FontSize="50" Foreground="#0078D4" Opacity="0.1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-5,0"/>
                            <StackPanel>
                                <TextBlock Text="ŚREDNIE TĘTNO" FontSize="9" FontWeight="Bold" Foreground="#90b5e0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding SelectedSession.AvgBpmText, TargetNullValue='--', FallbackValue='--'}" FontSize="24" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Bottom"/>
                                    <TextBlock Text=" BPM" FontSize="11" Margin="2,0,0,2" Foreground="#90b5e0" VerticalAlignment="Bottom"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ui:Card>

                    <!-- Card 3: Min / Max BPM -->
                    <ui:Card Margin="0,0,8,8" MinWidth="150" Padding="12" Background="#131116" BorderBrush="#A020F0" BorderThickness="1.5,1,1,1">
                        <Grid>
                            <ui:SymbolIcon Symbol="ArrowSort24" FontSize="50" Foreground="#A020F0" Opacity="0.1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-5,0"/>
                            <StackPanel>
                                <TextBlock Text="MIN / MAX TĘTNO" FontSize="9" FontWeight="Bold" Foreground="#c690e0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding SelectedSession.MinMaxString, TargetNullValue='-- / --', FallbackValue='-- / --'}" FontSize="20" FontWeight="Bold" Foreground="#A020F0" VerticalAlignment="Bottom"/>
                                    <TextBlock Text=" BPM" FontSize="11" Margin="2,0,0,1" Foreground="#c690e0" VerticalAlignment="Bottom"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ui:Card>

                    <!-- Card 4: Monitoring Duration -->
                    <ui:Card Margin="0,0,8,8" MinWidth="150" Padding="12" Background="#111512" BorderBrush="#107C41" BorderThickness="1.5,1,1,1">
                        <Grid>
                            <ui:SymbolIcon Symbol="Timer24" FontSize="50" Foreground="#107C41" Opacity="0.1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-5,0"/>
                            <StackPanel>
                                <TextBlock Text="CZAS MONITOROWANIA" FontSize="9" FontWeight="Bold" Foreground="#90e0a5"/>
                                <TextBlock Text="{Binding SelectedSession.DurationString, TargetNullValue='00:00:00', FallbackValue='00:00:00'}" FontSize="20" FontWeight="Bold" Foreground="#107C41" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>

                    <!-- Card 5: Calories Burned -->
                    <ui:Card Margin="0,0,0,8" MinWidth="150" Padding="12" Background="#161511" BorderBrush="#D89B00" BorderThickness="1.5,1,1,1">
                        <Grid>
                            <ui:SymbolIcon Symbol="Fire24" FontSize="50" Foreground="#D89B00" Opacity="0.1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-5,0"/>
                            <StackPanel>
                                <TextBlock Text="SPALONE KALORIE" FontSize="9" FontWeight="Bold" Foreground="#e0c690"/>
                                <TextBlock Text="{Binding SelectedSession.CaloriesBurnedText, TargetNullValue='0 kcal', FallbackValue='0 kcal'}" FontSize="20" FontWeight="Bold" Foreground="#D89B00" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </WrapPanel>

                <!-- Historical Chart Card -->
                <ui:Card Grid.Row="2" Margin="0" Padding="16" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="#111116" BorderBrush="#1D1D26" MinHeight="350" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Wykres Historyczny" FontSize="16" FontWeight="SemiBold" Foreground="#f0f0f5" />
                                <TextBlock Text="Pełny przebieg zapisanego treningu" FontSize="12" Foreground="#a0a0b0" Margin="0,2,0,0" />
                            </StackPanel>
                        </Grid>
                        <Canvas Grid.Row="1" x:Name="ChartCanvasHistory" ClipToBounds="True" Background="#0C0C12" SizeChanged="ChartCanvasHistory_SizeChanged" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                    </Grid>
                </ui:Card>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
